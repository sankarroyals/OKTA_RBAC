import {createTheme} from '@mui/material/styles';

import palette from './palette';
import typography from './typography';

const customealltheme = createTheme({
  palette,
  typography,
});

export default customealltheme;
